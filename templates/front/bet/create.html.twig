{% extends 'base_front.html.twig' %}

{% block title %}Paris de {{ user.nickname }}{% endblock %}

{% block body %}


<div class="container mt-5">
    <div class="d-flex ">
        <h1 class="flex-shrink-0">Mes crédits - </h1>
        <p class="flex-grow-1 ms-2 m-auto fs-5">{{ user.credits ?? 0 }} credits<p>
    </div>

    <div class="d-flex justify-content-left mb-7 mt-5">
            <div class="card card-profile shadow-sm " style="width:80%">
                <div class="card-body">
                    {{ form(form) }}
                </div>
            </div>
    </div>
</div>

{% endblock %}
