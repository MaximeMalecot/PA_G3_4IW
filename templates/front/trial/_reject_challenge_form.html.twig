<form method="post" action="{{ path('front_trial_challenge_refuse', {'id': trial.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('refuseChallenge' ~ trial.id) }}">
    <button class="btn btn-bettle">Refuser</button>
</form>
