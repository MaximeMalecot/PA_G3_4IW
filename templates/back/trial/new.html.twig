<!DOCTYPE html>

<title>New Trial</title>

{% block body %}
    <h1>Create new Trial</h1>

    {{ include('back/trial/_form.html.twig') }}

    <a href="{{ path('back_trial_index') }}">back to list</a>
{% endblock %}
