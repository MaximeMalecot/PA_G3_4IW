<!DOCTYPE html>

<title>Edit Trial</title>

{% block body %}
    <h1>Edit Trial</h1>

    {{ include('back/trial/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('back_trial_index') }}">back to list</a>

    {{ include('back/trial/_delete_form.html.twig') }}
{% endblock %}
