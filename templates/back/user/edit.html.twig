{% extends 'base_back.html.twig' %}

{% block title %}Bettle - Modifier Utilisateur{% endblock %}

{% block body %}

    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Modifier l'utilisateur</h3>
        </div>
        <div class="card-body">
            {{ include('back/user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            {{ include('back/user/_pwd_edit_form.html.twig') }}

        </div>
        <div class="card-footer d-flex flex-column gap-2">
            {{ include('back/user/_delete_form.html.twig') }}

            <a href="{{ path('back_user_index') }}">
                <button class="btn btn-secondary w-100">Retour à la liste</button>
            </a>
        </div>
    </div>


{% endblock %}
